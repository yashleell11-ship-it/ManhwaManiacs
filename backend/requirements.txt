fastapi==0.138.2
uvicorn==0.49.0
python-multipart==0.0.32
curl_cffi==0.15.0
pydantic==2.13.4
pydantic-settings==2.14.2
sqlalchemy==2.0.52
alembic==1.18.5
httpx==0.28.1
argon2-cffi==25.1.0
slowapi==0.1.10
# Cover downscaling only (GET /sources/.../cover?w=). Prebuilt manylinux
# wheel with libwebp + libjpeg-turbo bundled, so the Dockerfile still needs no
# apt packages; ~7 MB installed. See services/image_resize.py.
pillow==12.3.0
# Narration for iOS: Ogg Opus -> AAC/M4A (services/chapter_audio_m4a.py).
# abi3 manylinux wheel with its own libav* bundled, so the Dockerfile still
# needs no apt packages (no ffmpeg); ~100 MB installed.
av==18.1.0
